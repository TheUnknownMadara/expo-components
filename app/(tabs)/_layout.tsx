import { Slot } from 'expo-router';

export default function Layout() {
  return (
    // This is the content of the layout that will be rendered by the router. Customize this to add your own header, footer, etc.
    <Slot />
  );
}
